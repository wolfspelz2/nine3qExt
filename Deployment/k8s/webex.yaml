apiVersion: apps/v1
kind: Deployment
metadata:
  name: n3q-webex
  namespace: n3q-prod
spec:
  selector:
    matchLabels:
      run: n3q-webex
  replicas: 2
  template:
    metadata:
      labels:
        run: n3q-webex
    spec:
      containers:
        - name: n3q-webex
          image: docker.k8s.sui.li/n3q-webex:1
          ports:
            - containerPort: 80
              protocol: TCP

---
apiVersion: v1
kind: Service
metadata:
  name: n3q-webex
  namespace: n3q-prod
  labels:
    run: n3q-webex
  annotations:
    haproxy.org/check: "enabled"
    haproxy.org/forwarded-for: "enabled"
    haproxy.org/load-balance: "roundrobin"
spec:
  selector:
    run: n3q-webex
  ports:
  - name: http
    port: 80
    protocol: TCP

---
apiVersion: networking.k8s.io/v1beta1
kind: Ingress
metadata:
  name: n3q-webex-ingress
  namespace: n3q-prod
spec:
  rules:
  - host: n3qwebex.k8s.sui.li
    http:
      paths:
      - path: /
        backend:
          serviceName: n3q-webex
          servicePort: 80
